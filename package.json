{
  "name": "workspaces",
  "private": true,
  "module": "packages/feed/index.ts",
  "type": "module",
  "scripts": {
    "feed:start": "bun --filter @app/feed start",
    "frontend:dev": "bun --filter @app/frontend dev",
    "build-frontend": "bun --filter @app/frontend build",
    "types": "bun run --filter '*' types"
  },
  "workspaces": [
    "packages/*"
  ]
}